readquery <- function()
{ 
prompt <- "Digite a consulta: "
  n <- readline(prompt)
  n <- (unlist(strsplit(n, "\\s+")))
  return(n)
}

consulta<- function() {
as.numeric(as.character(item1[item1$Termos==(readquery())[1],c('idf')]))
}
