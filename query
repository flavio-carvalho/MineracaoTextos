readquery <- function()
{ 
prompt <- "Digite a consulta: "
  n <- readline(prompt)
  n <- (unlist(strsplit(n, "\\s+")))
  return(n)
}

consulta<- function() {
rq<-readquery()

for (i in 1:length(rq)){
	print(as.numeric(as.character(item1[item1$Termos==rq[i],c('idf')])))
}
}
